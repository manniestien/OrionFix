"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var react_1 = __importDefault(require("react"));
var utils_1 = require("../utils");
exports.Line = function (_a) {
    var data = _a.data, _b = _a.pathVariables, rx = _b.rx, ry = _b.ry, height = _b.height, moveElement = _b.moveElement, strokeColor = _b.strokeColor;
    var move = function () { return moveElement(data.startAngle); };
    var styles = { fill: data.color, cursor: 'pointer' };
    var middle = utils_1.calculateMiddleAngle(data.startAngle, data.endAngle);
    var x1 = rx * Math.cos(middle);
    var y1 = ry * Math.sin(middle);
    return (react_1.default.createElement(react_1.default.Fragment, null,
        react_1.default.createElement("line", { x1: x1, y1: y1, x2: x1 + 20 * (middle > 3 / 2 * Math.PI || middle < Math.PI / 2 ? 1 : -1), y2: y1 + height / 2 * (middle > Math.PI ? -1 : 1), stroke: "black", transform: data.moved ? "translate(" + utils_1.calculateNewPosition(data, rx, ry) + ")" : 'translate(0,0)' }),
        react_1.default.createElement("line", { x1: x1 + 14 * (middle > 3 / 2 * Math.PI || middle < Math.PI / 2 ? 1 : -1), y1: y1 + height / 2 * (middle > Math.PI ? -1 : 1), x2: (rx + 20) * (middle > 3 / 2 * Math.PI || middle < Math.PI / 2 ? 1 : -1), y2: y1 + height / 2 * (middle > Math.PI ? -1 : 1), stroke: "black", transform: data.moved ? "translate(" + utils_1.calculateNewPosition(data, rx, ry) + ")" : 'translate(0,0)' })));
};

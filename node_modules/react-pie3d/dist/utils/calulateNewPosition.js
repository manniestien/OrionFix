"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var calculateMiddleAngle_1 = require("./calculateMiddleAngle");
exports.calculateNewPosition = function (data, rx, ry) {
    var middleAngle = calculateMiddleAngle_1.calculateMiddleAngle(data.startAngle, data.endAngle);
    return [
        0.2 * rx * Math.cos(middleAngle > Math.PI ? middleAngle : -middleAngle),
        0.2 * ry * Math.sin(middleAngle),
    ];
};

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createOuterPath = function (data, rx, ry, hight) {
    var startAngle = (data.startAngle > Math.PI ? Math.PI : data.startAngle);
    var endAngle = (data.endAngle > Math.PI ? Math.PI : data.endAngle);
    var sx = rx * Math.cos(startAngle);
    var sy = ry * Math.sin(startAngle);
    var ex = rx * Math.cos(endAngle);
    var ey = ry * Math.sin(endAngle);
    return "M " + sx + " " + (hight + sy) + " A " + rx + " " + ry + " 0 0 1 " + ex + " " + (hight + ey) + " L " + ex + " " + ey + " A " + rx + " " + ry + " 0 0 0 " + sx + " " + sy + " z";
};

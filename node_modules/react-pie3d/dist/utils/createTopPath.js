"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createTopPath = function (data, rx, ry, ir) {
    var endAngle = data.endAngle, startAngle = data.startAngle;
    if (endAngle - startAngle === 0) {
        return 'M 0 0';
    }
    var sx = rx * Math.cos(startAngle);
    var sy = ry * Math.sin(startAngle);
    var ex = rx * Math.cos(endAngle);
    var ey = ry * Math.sin(endAngle);
    return "M " + sx + " " + sy + " A " + rx + " " + ry + " 0 " + (endAngle - startAngle > Math.PI ? 1 : 0) + " 1 " + ex + " " + ey + " L " + ir * ex + " " + ir * ey + " A " +
        (ir * rx + " " + ir * ry + " 0 " + (endAngle - startAngle > Math.PI ? 1 : 0) + " 0 " + ir * sx + " " + ir * sy + " z");
};

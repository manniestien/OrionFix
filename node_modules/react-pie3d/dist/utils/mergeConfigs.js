"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mergeConfigs = function (config, defaultConfig) { return ({
    angle: config.angle || defaultConfig.angle,
    height: config.height || defaultConfig.height,
    ir: config.ir || defaultConfig.ir,
    showLabels: config.showLabels || defaultConfig.showLabels,
    showTooltips: config.showTooltips || defaultConfig.showTooltips,
}); };
